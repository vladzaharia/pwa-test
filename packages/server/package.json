{
  "name": "server",
  "version": "1.0.0",
  "description": "Simple API server for PWA demo app",
  "main": "server.js",
  "scripts": {
    "dev": "nodemon server.js",
    "build": "echo 'Server: No build step required'",
    "start": "node server.js",
    "test": "echo 'Server: No tests configured'",
    "lint": "eslint . --report-unused-disable-directives --max-warnings 0",
    "lint:fix": "eslint . --fix",
    "format": "prettier --write .",
    "format:check": "prettier --check .",
    "clean": "echo 'Server: No build artifacts to clean'"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "express": "^4.18.2"
  },
  "devDependencies": {
    "@eslint/js": "^9.29.0",
    "eslint": "^9.29.0",
    "globals": "^16.2.0",
    "nodemon": "^3.0.2",
    "prettier": "^3.6.1"
  },
  "engines": {
    "node": ">=16.0.0"
  }
}
